<div class="well no-padding">
    <%= form_for(@nature_financing, :html => { class: 'form-horizontal' }) do |f| %>
        
        <% if @nature_financing.errors.any? %>
            <div class="alert alert-error">
                <h4><%= pluralize(@nature_financing.errors.count, "error") %> prohibited this pessoa from being saved:</h4>
                <ul>
                    <% @nature_financing.errors.full_messages.each do |message| %>
                        <li><%= message %></li>
                    <% end %>
                </ul>
            </div>
        <% end %>

            <div class="control-group span6 no-margin-left">
                <%= f.label :title, 'Título:', class: 'control-label' %>
                <div class="controls">
                    <%= f.text_field :title, required: true, class: 'input-block-level' %>
                </div>
            </div>
            <div class="control-group span6">
                <%= f.label :institute, 'Instituição:', class: 'control-label' %>
                <div class="controls">
                    <%= f.select :institute_id, options_for_select(Institute.all.map{|s|[s.name, s.id]}, :selected => @nature_financing.institute_id), {:include_blank => '- Selecione -'}, { :class => 'span12', :required => true } %>
                </div>
            </div>
            <div class="control-group span12 no-margin-left">
                <%= f.label :description, 'Descrição:', class: 'control-label' %>
                <div class="controls">
                    <%= f.text_area :description, required: true, class: 'input-block-level' %>
                </div>
            </div>
            
        <div class="clearfix"></div>
        <div class="form-actions">
            <%= f.submit ( f.object.new_record? ? "Cadastrar" : "Atualizar"), class: "btn btn-primary" %>
        </div>
    <% end %>
</div>
