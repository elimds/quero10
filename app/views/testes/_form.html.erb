<div class="well no-padding">
    <%= form_for(@testis, :html => { class: 'form-horizontal' }) do |f| %>
        
        <% if @testis.errors.any? %>
            <div class="alert alert-error">
                <h4><%= pluralize(@testis.errors.count, "error") %> prohibited this pessoa from being saved:</h4>
                <ul>
                    <% @testis.errors.full_messages.each do |message| %>
                        <li><%= message %></li>
                    <% end %>
                </ul>
            </div>
        <% end %>

                    <div class="control-group span6 no-margin-left">
                <%= f.label :codigo, class: 'control-label' %>
                <div class="controls">
                    <%= f.number_field :codigo, required: true, class: 'input-block-level' %>
                </div>
            </div>
                    <div class="control-group span6 no-margin-left">
                <%= f.label :nome, class: 'control-label' %>
                <div class="controls">
                    <%= f.text_field :nome, required: true, class: 'input-block-level' %>
                </div>
            </div>
            
        <div class="form-actions">
            <%= f.submit 'Cadastrar', class: 'btn btn-primary' %>
        </div>
    <% end %>
</div>
