<div class="row-fluid">
    <div class="span12">
        <div class="top-bar">
            <h3><i class="icon-list-alt"></i> Visualizar Speciality</h3>
        </div>
        <div class="well no-padding">
            <% unless notice.nil? %>
                <div class="alert"><%= notice %></div>
            <% end %>
                            <div class="control-group span12 no-margin-left">
                    <label class="control-label">Title:</label>
                    <div class="controls">
                        <%= @speciality.title %>
                    </div>
                </div>
                            <div class="control-group span12 no-margin-left">
                    <label class="control-label">Description:</label>
                    <div class="controls">
                        <%= @speciality.description %>
                    </div>
                </div>
                            <div class="control-group span12 no-margin-left">
                    <label class="control-label">Sub area:</label>
                    <div class="controls">
                        <%= @speciality.sub_area %>
                    </div>
                </div>
                    </div>
    </div>
</div>
